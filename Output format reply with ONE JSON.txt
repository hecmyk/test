Output format: reply with ONE JSON object and nothing else.

Do NOT wrap the JSON in markdown code fences (no ```json, no ```).
Do NOT add any text before or after the JSON.
Do NOT add explanations, apologies, or preambles.

Your entire response must start with { and end with }.

Exact structure:
{"reasoning": "one or two sentences", "score": 0 or 1}